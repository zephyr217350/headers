#include <cmath>
int pole_trojkata (int a, int h)
{
    return (a*h)/2;
}

int potega (int liczba, int power)
{
    return pow(liczba, power);
}

int pierwiastek (int b)
{
    return sqrt(b);
}
